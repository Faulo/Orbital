using UnityEngine;

public class PlanetPhysic : MonoBehaviour {

    public float mass = 1;

    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
